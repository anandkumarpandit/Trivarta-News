{
    "name": "news-portal-root",
    "version": "1.0.0",
    "private": true,
    "engines": {
        "node": ">=18.0.0"
    },
    "scripts": {
        "install-all": "npm install && cd frontend && npm install && cd ../backend && npm install",
        "build": "cd frontend && npm install && rm -rf dist && npm run build && cd ../backend && npm install",
        "start": "node backend/server.js",
        "heroku-postbuild": "npm run build"
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    }
}